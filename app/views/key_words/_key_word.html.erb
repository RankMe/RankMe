<tr>
  <td style="vertical-align:middle;">
    <%= key_word.string %>
  </td>
  <td style="text-align:center;vertical-align:middle;">
    <% if key_word.rank <= 0 %>
      -
    <% else %>
      <%= key_word.rank.to_s %> 
    <% end %>
  </td>
  <td>
    <%
      url = "/projects/" + key_word.site.project.id.to_s + "/sites/" + key_word.site.id.to_s + "/keywords/" + key_word.id.to_s
    %>
    <a class="btn btn-primary" href="<%= url %>">Détails</a>
    <a class="btn btn-danger" data-confirm="Êtes-vous sûr de vouloir supprimer ce mot clef?" data-method="delete" href="<%= url %>" rel="nofollow"><b>X</b></a>
	</td>
</tr>